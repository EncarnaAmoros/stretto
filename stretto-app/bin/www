#!/usr/bin/env node

var app = require('../app');
var debug = require('debug')('.bin:server');
var models = require('../models');

app.set('port', process.env.PORT || 3000);

/* Llamada al modelo */

models.sequelize.sync({force:true}).then(function () {
  var server = app.listen(app.get('port'), function() {
    debug('Express server listening on port ' + server.address().port);
  });
  console.log('do it!!!!!!!!!!!!!!!!');
});





/* Rellenando BD */

/*
models.Articulo.create({
    nombre: "aaa"
}).then(function() {
    console.log('fiinininine!!!!!!!!!!!!!!!!');
});
models.Tipo.create({
    nombre: 'Cuerda'
}).then(function() {
    models.Tipo.create({
        nombre: 'Viento'
    }).then(function() {
        console.log('Cat hecha');
    });
});
models.Tipo.bulkCreate([
		{nombre:'Cuerda'},
		{nombre:'Viento'}
]);
models.Articulo.bulkCreate([
		{nombre:'Guitarra', TipoId: 1},
		{nombre:'Flauta', TipoId: 2}
]);*/